# Vision-Agentic RAG: Μια Νέα Αρχιτεκτονική για την Ελληνική Νομοθεσία

Αυτό το έγγραφο περιγράφει την πρόταση για τη μετάβαση από το κλασικό Text-based RAG σε ένα **Vision-Agentic RAG** σύστημα, το οποίο επιλύει τα προβλήματα του αργού OCR και της απώλειας πληροφορίας κατά το text extraction.

## Το Πρόβλημα με την Τρέχουσα Προσέγγιση
1. **Extraction Bottleneck:** Η προσπάθεια μετατροπής πολύπλοκων PDF (όπως τα ΦΕΚ, που περιέχουν πίνακες, σφραγίδες, κακή ποιότητα σάρωσης) σε καθαρό κείμενο (Markdown) είναι χρονοβόρα, υπολογιστικά ακριβή και συχνά αναξιόπιστη (χάνονται δομές και αριθμοί).
2. **Διπλοαποθήκευση (Storage Overhead):** Αποθηκεύουμε τα δεδομένα και στο PDF και στη βάση (ως κείμενο/chunks), αυξάνοντας τις απαιτήσεις χώρου.
3. **Απώλεια Πλαισίου (Context Loss):** Το κόψιμο των κειμένων σε ανεξάρτητα chunks των 512 tokens καταστρέφει τη δομική συνέχεια των νομικών κειμένων (Άρθρο -> Παράγραφος).

## Η Προτεινόμενη Λύση: Vision-Agentic RAG
Η κεντρική ιδέα είναι: **Το PDF παραμένει η μόνη Πηγή Αλήθειας (Source of Truth). Το σύστημα χτίζει μόνο "Δείκτες" (Pointers) και διαβάζει το PDF οπτικά την ώρα της ερώτησης.**

Η αρχιτεκτονική χωρίζεται σε δύο φάσεις:

### Φάση 1: Ελαφρύ Ingestion (Μόνο για Ευρετηρίαση)
Αντί για πλήρες OCR και text extraction, το σύστημα κάνει ένα γρήγορο πέρασμα για να δημιουργήσει έναν **Χάρτη Πλοήγησης**:
* Εξάγει μόνο τα βασικά (Τίτλους Άρθρων, Αριθμούς Νόμων) και τα συνδέει με τη **σελίδα του PDF**.
* Δημιουργεί ένα ελαφρύ δομημένο Ευρετήριο (Index) της μορφής:
  `[Νόμος 5090/2024] -> [Άρθρο 5] -> [Σελίδα 12 του 2024/A/30.pdf]`
* Εξάγει χαμηλής ποιότητας κείμενο/embeddings **αποκλειστικά για αναζήτηση** στο παρασκήνιο (δεν θα δοθεί ποτέ ως έχει στο LLM).

### Φάση 2: Agentic Reading (Την ώρα της ερώτησης)
Όταν ο χρήστης ρωτά κάτι:
1. **Search & Locate:** Το Retrieval Layer βρίσκει το σχετικό ΦΕΚ και την ακριβή **σελίδα** (μέσω του Ευρετηρίου και των Embeddings).
2. **Visual Tool Call:** Ο Agent λαμβάνει την εντολή να διαβάσει τις συγκεκριμένες σελίδες: π.χ., `read_pdf_pages(file="2024/A/30.pdf", pages=[12, 13])`.
3. **Multimodal Analysis:** Το LLM (π.χ. GPT-4o) λαμβάνει τις σελίδες 12 και 13 ως **εικόνες**. Διαβάζει το αληθινό, πρωτότυπο έγγραφο με τη δομή του ανέπαφη (πίνακες, υποσημειώσεις).
4. **Απάντηση:** Συνθέτει την απάντηση βασισμένο στην οπτική ανάλυση της πηγής.

## Πλεονεκτήματα
* **Zero Information Loss:** Το μοντέλο βλέπει ακριβώς ό,τι θα έβλεπε ένας άνθρωπος, λύνοντας το πρόβλημα των κακοφτιαγμένων πινάκων στα ΦΕΚ.
* **Ταχύτητα Ingestion:** Το σκανάρισμα για δημιουργία ευρετηρίου είναι τάχιστο συγκριτικά με το να προσπαθείς να σώσεις τέλειο Markdown.
* **Ακρίβεια (Accuracy):** Εξαλείφονται τα hallucinations που προέρχονται από κακά chunks.

## Επόμενα Βήματα Υλοποίησης
1. Προσαρμογή του `docling` (ή χρήσης pdf2image) ώστε να αποθηκεύει page numbers αντί για απλά chunks.
2. Τροποποίηση του Supabase Schema για να φιλοξενεί τα `legal_units` (Άρθρα/Παράγραφοι) συνδεδεμένα με σελίδες PDF.
3. Δημιουργία ενός νέου Agent Tool (π.χ. `VisionReaderAgent`) που θα αναλαμβάνει να "κοιτάξει" το PDF βάσει του index.