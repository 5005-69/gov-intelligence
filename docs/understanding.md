# Understanding the Greek Legislation RAG Codebase

Αυτό το έγγραφο περιγράφει την αρχιτεκτονική και τη ροή του συστήματος "Greek Legislation RAG", χωρισμένη σε κύριες φάσεις. Το σύστημα έχει σχεδιαστεί για να διαβάζει ΦΕΚ (PDF), να τα επεξεργάζεται με OCR όπου χρειάζεται, να τα αποθηκεύει σε μια διανυσματική βάση (Supabase) και να απαντά σε ερωτήσεις χρησιμοποιώντας πολλαπλούς πράκτορες (Agents).

---

## 1. Ingestion Phase: Από το PDF στη Βάση Δεδομένων
Αυτή η φάση αναλαμβάνει τη μετατροπή των εγγράφων σε αναζητήσιμα chunks.

*   **PDF Extraction & OCR Cluster** (`src/pdf/`)
    *   `docling_loader.py`: Χρησιμοποιεί τη βιβλιοθήκη Docling για να διαβάσει τη δομή του PDF (κείμενο, πίνακες κλπ).
    *   `ocr.py`: Αν το PDF είναι σκαναρισμένο και δεν έχει κείμενο, ενεργοποιεί το `ocrmypdf` και το `Tesseract` για να "διαβάσει" την εικόνα.
*   **Processing Cluster** (`src/ingestion/`)
    *   `ingest.py`: Ο "ενορχηστρωτής" της εισαγωγής. Διαβάζει τους φακέλους, καλεί τον loader και στέλνει τα δεδομένα για αποθήκευση.
    *   `chunker.py`: Σπάει το μεγάλο κείμενο σε μικρότερα κομμάτια (chunks) ώστε να μπορούν να επεξεργαστούν από το RAG.
    *   `metadata.py`: Εμπλουτίζει κάθε PDF με πληροφορίες όπως έτος, αριθμό ΦΕΚ και τίτλο (με τη βοήθεια LLM αν χρειαστεί).
    *   `embedder.py`: Μετατρέπει το κείμενο σε διανύσματα (embeddings) χρησιμοποιώντας το OpenAI API.
    *   `diagnose.py`: Εργαλείο ελέγχου για να βλέπουμε αν η εξαγωγή κειμένου δουλεύει σωστά σε ένα συγκεκριμένο PDF.

---

## 2. Storage Phase: Διαχείριση Δεδομένων
*   **Database Cluster** (`src/retrieval/` & `sql/`)
    *   `sql/001_init.sql`: Το σενάριο που στήνει τη βάση στο Supabase (πίνακες, vector indexes, hybrid search functions).
    *   `store.py`: Η γέφυρα του Python κώδικα με το Supabase. Αναλαμβάνει το "γράψιμο" των chunks και των διανυσμάτων στη βάση.

---

## 3. RAG Phase: Το Σύστημα Πρακτόρων (Agents)
Όταν ο χρήστης κάνει μια ερώτηση, ενεργοποιείται ένα γράφημα πρακτόρων (LangGraph) που συνεργάζονται για την απάντηση.

*   **Coordination & Logic** (`src/rag/`)
    *   `graph.py`: Ορίζει τη ροή (το γράφημα). Ποιος πράκτορας μιλάει σε ποιον και με ποια σειρά.
    *   `retriever.py`: Υλοποιεί την υβριδική αναζήτηση (semantic + full-text search) στη βάση.
    *   `state.py`: Κρατάει τη μνήμη και την κατάσταση της συζήτησης μέσα στο γράφημα.
*   **Agents Cluster** (`src/rag/agents/`)
    *   `rewriter_agent.py`: Ξαναγράφει την ερώτηση του χρήστη ώστε να είναι πιο σαφής για την αναζήτηση.
    *   `listing_agent.py`: Ειδικεύεται στην αναζήτηση μέσω των manifest αρχείων (λίστες ΦΕΚ) για να βρει συγκεκριμένα έγγραφα βάσει αριθμού/έτους.
    *   `chunk_agent.py`: Αναζητά μέσα στο περιεχόμενο των εγγράφων για να βρει συγκεκριμένες απαντήσεις.
    *   `combiner_agent.py`: Συγκεντρώνει όλες τις πληροφορίες από τους άλλους πράκτορες και συνθέτει την τελική απάντηση με παραπομπές.
    *   `base_agent.py`: Περιέχει την κοινή λογική που μοιράζονται όλοι οι πράκτορες.

---

## 4. Interaction Phase: Η Επαφή με το Χρήστη
*   `src/main.py`: Το κεντρικό σημείο εισόδου (CLI). Από εδώ ο χρήστης τρέχει εντολές για `ingest`, `query`, ή `stats`.
*   `src/config.py`: Διαχειρίζεται τις ρυθμίσεις του συστήματος (API keys, μοντέλα, κατώφλια OCR).

---

## Γενική Ροή Δεδομένων (Data Flow)
1.  **Ingest**: `PDF` -> `Docling/OCR` -> `Chunking` -> `Embedding` -> `Supabase`.
2.  **Query**: `User Question` -> `Rewriter` -> (`Listing Agent` & `Chunk Agent`) -> `Retriever` -> `Combiner` -> `Final Answer`.
